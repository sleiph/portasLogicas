using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class areaDeTrabalho : MonoBehaviour
{
    public GameObject conector;

    void OnMouseDown()
    {
        /*if (linhaColisao.isOverConector != true){
            Camera c = Camera.main;
            Vector3 p = c.ScreenToWorldPoint (new Vector3 (Input.mousePosition.x, Input.mousePosition.y, 10));
            linhaColisao.linePoints[0] = p;
            Instantiate(conector, p, Quaternion.identity);
        }*/
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}